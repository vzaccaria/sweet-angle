
directive xy {
	import a, b;
	byref c,d;
	byval e,f;
	callback g,h;

	template `div`;

	create(e,a) { }
}

extend xy into za {
    import;
    byref k,u;
    byval j;
    callback h;
    template `
span!=parent`;
    create(z,u) { }
}

commitDirective xy;
commitDirective za;
